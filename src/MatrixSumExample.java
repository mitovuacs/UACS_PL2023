//This example initializes a 2D array and then calculates the sum of all its elements. The students will need to fill in the gaps to sum the values.
public class MatrixSumExample {
    public static void main(String[] args) {
        // Initialize a 2D array (matrix)
        int[][] matrix = {
                {1, 2, 3},
                {4, 5, 6},
                {7, 8, 9}
        };

        int sum = 0;

        // Loop to go through each row
        for(/*                              */) {
            // Loop to go through each column in a row
            for(/*                                 */) {
                // Fill in the gap to add the matrix element to the sum
                sum = /*                     */;
            }
        }

        // Print the sum of all elements in the matrix
        System.out.println("The sum of all elements in the matrix is: " + sum);
    }
}
